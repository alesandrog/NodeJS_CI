name: CI nodeJS
on:
  push:
    branches: [ feature/server ]
jobs:
  ci_to_develop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with: 
          node-version: 14
      - run: cd server
      - run: cd src
      - run: npm install
      - run: npm run test

      